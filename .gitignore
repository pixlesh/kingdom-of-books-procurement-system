# === Dependencies (frontend + backend) ===
node_modules/

# === Environment files — never commit secrets ===
.env
.env.local
.env.*.local
# The documented template stays tracked
!.env.example

# === Server data store — 254 catalog records + covers, ships via a persistent
# disk on Render, never via git (see PROJECT_HANDOFF.md §7-8) ===
server/data/

# === Build outputs ===
dist/
dist-ssr/
build/
*.local

# === Logs ===
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# === Editor / IDE ===
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# === OS files ===
.DS_Store
Thumbs.db
desktop.ini
