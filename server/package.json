{
  "name": "kingdom-of-books-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Lightweight API layer for the Kingdom of Books internal tool. Owns all third-party API keys and requests (Google Books, Open Library, Gemini AI, future OCR) so the React frontend never talks to external services directly.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "morgan": "^1.10.0"
  }
}
